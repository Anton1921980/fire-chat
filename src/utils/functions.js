import firebase from 'firebase';
import 'firebase/database';
import React from 'react';

// export function startPersonalChat(e) {
//     e.preventDefault();

//     setFriend(e.target.innerText)
//     setChatId([user.displayName, e.target.innerText].sort().join(''))
// }
